# evervim.vim

## 概要:
edit evernote on vim.

## 要件:
python及びpythonを有効にしてコンパイルしたvimが必要です。
vimのコマンドラインから:echo has('python')と実行し、1が表示されていれば利用できます。

## TODO:
・リスト表示次の戻る(Ctrl-o)などの実装
・まだ文字化けかなにかでうまくいかないケースがある。解明と対策
